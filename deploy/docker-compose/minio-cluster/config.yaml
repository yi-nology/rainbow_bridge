server:
  address: ":8080"

database:
  driver: "postgres"
  postgres:
    dsn: "host=postgres user=rainbow_bridge password=rainbow_bridge_pass dbname=rainbow_bridge port=5432 sslmode=disable TimeZone=Asia/Shanghai"

# TODO: MinIO storage 功能尚未实现，需要后续在 Config 结构体中添加 Storage 字段
# storage:
#   type: "minio"
#   minio:
#     endpoint: "nginx:9000"
#     access_key: "minioadmin"
#     secret_key: "minioadmin123"
#     use_ssl: false
#     bucket: "rainbow-bridge"
#     region: "us-east-1"

upload:
  path: "/var/lib/rainbow_bridge/uploads"
  max_size: 10485760  # 10MB

redis:
  enabled: true
  address: "redis:6379"
  password: ""
  db: 0

log:
  level: "info"
  output: "stdout"
