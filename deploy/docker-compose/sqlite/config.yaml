server:
  address: ":8080"

database:
  driver: "sqlite"
  sqlite:
    path: "/var/lib/rainbow_bridge/resource.db"

upload:
  max_size: 10485760  # 10MB

redis:
  enabled: true
  address: "redis:6379"
  password: ""
  db: 0
