{
  "name": "rainbow-bridge-e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for Rainbow Bridge",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report reports/html",
    "test:codegen": "playwright codegen http://localhost:8080"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.0",
    "@types/node": "^20.11.5",
    "typescript": "^5.3.3"
  }
}
